{
  "installCommand": "cd server && npm install && cd ../client && npm install",
  "buildCommand": "cd server && mkdir -p ../api && npx esbuild src/vercel-handler.ts --bundle --platform=node --outfile=../api/index.js --format=cjs && cd ../client && npx vite build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" }
  ]
}
